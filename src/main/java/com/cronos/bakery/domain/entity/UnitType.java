package com.cronos.bakery.domain.entity;

public enum UnitType {
    WEIGHT,      // kg, g, lb, oz
    VOLUME,      // l, ml, gal, cup
    PIECE,       // units, dozen
    CONTAINER    // bag, bottle, box, jar
}
